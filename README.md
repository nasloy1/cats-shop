# 🐱 Telegram Mini App — Магазин котов

Полноценное Telegram Mini App для продажи котят с каталогом, корзиной, формой заказа и обратной связью.

## Структура проекта

```
telegram-cats-shop/
├── frontend/
│   ├── index.html    ← Главная страница (SPA)
│   ├── style.css     ← Стили (темы Telegram)
│   └── app.js        ← Логика + каталог котов
└── bot/
    ├── bot.py        ← Telegram бот (Python)
    ├── requirements.txt
    ├── .env.example  ← Шаблон настроек
    └── .env          ← Ваши секреты (не коммитить!)
```

## Функциональность

- **Каталог** — карточки котов с фото, породой, ценой, возрастом
- **Фильтры** — по породе + поиск по имени/породе/окрасу
- **Детальная страница** — полное описание, теги, кнопка "В корзину"
- **Корзина** — список выбранных котят, итоговая сумма
- **Оформление заказа** — форма с именем, телефоном, адресом
- **Обратная связь** — форма с темой и сообщением
- **О нас** — информация о питомнике, контакты
- **Автоматические уведомления** — бот отправляет заказы/обращения администратору

---

## Шаг 1 — Создать бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Задайте имя и username бота
4. Скопируйте токен (вида `1234567890:AAF...`)

---

## Шаг 2 — Разместить фронтенд (HTTPS обязателен!)

### Вариант A: GitHub Pages (бесплатно, рекомендуется)

1. Создайте аккаунт на [github.com](https://github.com)
2. Создайте новый репозиторий (например `cats-shop`)
3. Загрузите файлы из папки `frontend/` в корень репозитория
4. В настройках репозитория: **Settings → Pages → Source: main branch → / (root)**
5. Ваш URL будет: `https://ВАШ_USERNAME.github.io/cats-shop`

### Вариант B: Netlify (бесплатно)

1. Зайдите на [netlify.com](https://netlify.com)
2. Перетащите папку `frontend/` в браузер
3. Получите URL вида `https://random-name.netlify.app`

### Вариант C: Любой хостинг с HTTPS

Загрузите файлы из `frontend/` через FTP/панель управления.

---

## Шаг 3 — Настроить бота

```bash
cd bot

# Скопировать шаблон настроек
cp .env.example .env
```

Откройте `.env` и заполните:
```env
BOT_TOKEN=ВАШ_ТОКЕН_ОТ_BOTFATHER
MINI_APP_URL=https://ВАШ_USERNAME.github.io/cats-shop
ADMIN_CHAT_ID=ВАШ_CHAT_ID
```

**Как узнать свой chat_id:** напишите боту [@userinfobot](https://t.me/userinfobot) — он пришлёт ваш ID.

---

## Шаг 4 — Запустить бота

```bash
cd bot

# Установить зависимости (один раз)
pip install -r requirements.txt

# Запустить
python bot.py
```

---

## Шаг 5 — Подключить Mini App к боту

1. Откройте [@BotFather](https://t.me/BotFather)
2. Выберите вашего бота → **Bot Settings → Menu Button → Configure menu button**
3. Вставьте URL вашего фронтенда

---

## Шаг 6 — Добавить своих котов

Откройте [frontend/app.js](frontend/app.js) и найдите массив `CATS` в самом начале файла.

Для каждого кота измените:
```js
{
  id: 1,
  name: 'Барсик',               // Кличка
  breed: 'Британская',          // Порода
  category: 'british',          // Категория фильтра
  age_months: 4,                // Возраст в месяцах
  gender: 'male',               // male / female
  price: 25000,                 // Цена в рублях
  color: 'Голубой',             // Окрас
  vaccinated: true,             // Привит?
  pedigree: true,               // Есть родословная?
  available: true,              // Доступен для продажи?
  description: 'Описание...',   // Подробное описание
  image: 'https://...',         // Ссылка на фото (HTTPS)
},
```

### Категории для фильтра
| category | Название |
|---|---|
| `british` | Британские |
| `persian` | Персидские |
| `maine-coon` | Мейн-куны |
| `scottish` | Шотландские |
| `siberian` | Сибирские |
| `ragdoll` | Рэгдоллы |

---

## Запуск бота на сервере (чтобы работал 24/7)

### Вариант: Railway.app (бесплатный тариф)

1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. Создайте проект → Deploy from GitHub
3. Добавьте переменные окружения в настройках проекта
4. Railway автоматически запустит `python bot.py`

### Вариант: Render.com

1. Зарегистрируйтесь на [render.com](https://render.com)
2. New → Background Worker → подключите репозиторий
3. Build command: `pip install -r bot/requirements.txt`
4. Start command: `python bot/bot.py`

---

## Тестирование без сервера (ngrok)

Для быстрого теста на локальном компьютере:

```bash
# Установите ngrok: https://ngrok.com/download
# Запустите фронтенд локально (например через Live Server в VS Code)
# Затем:
ngrok http 5500
```

Скопируйте HTTPS URL из ngrok и вставьте в `MINI_APP_URL`.

---

## Изменение информации о питомнике

Откройте `frontend/index.html` и найдите раздел `page-about`:
- Название питомника
- Контактный телефон и email
- Адрес
- Режим работы
- Список пород

---

## Требования

- Python 3.10+
- pip